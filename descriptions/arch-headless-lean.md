Arch Linux headless (rolling). A deliberately minimal netboot base: base
system + kernel/firmware + python + the dracut ramboot machinery, with NO dev
toolchain. A leaner live-env base than a slimmed Ubuntu, tracking a rolling
release.
